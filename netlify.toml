[build]
# Use Corepack to activate the same pnpm we used to generate the lockfile
command = "corepack enable && corepack prepare pnpm@8.15.4 --activate && pnpm install --frozen-lockfile && pnpm build"

[build.environment]
NODE_VERSION = "20.19.5"

[[plugins]]
package = "@netlify/plugin-nextjs"
